<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>EasyFlow</title>

    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: Arial, sans-serif;
            background: #f4f7fb;
            color: #111827;
        }

        /* ================= HEADER ================= */

        .header {
            width: 100%;
            background: #ffffff;
            border-bottom: 1px solid #e5e7eb;

            position: sticky;
            top: 0;
            z-index: 9999;
        }

        .header-inner {
            max-width: 1200px;
            margin: 0 auto;

            min-height: 72px;
            padding: 10px 20px;

            display: flex;
            align-items: center;
            justify-content: space-between;

            gap: 20px;
        }

        /* ЛОГО */

        .logo {
            color: #2563eb;
            font-size: 28px;
            font-weight: 800;

            cursor: pointer;
            white-space: nowrap;
        }

        /* ================= NAV ================= */

        .nav {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .nav-button {
            border: none;
            outline: none;

            background: transparent;

            color: #374151;

            padding: 12px 14px;

            border-radius: 10px;

            font-size: 15px;
            font-weight: 600;

            cursor: pointer;

            transition: all 0.2s ease;

            white-space: nowrap;
        }

        .nav-button:hover {
            background: #eff6ff;
            color: #2563eb;
        }

        .nav-button.active {
            background: #2563eb;
            color: white;
        }

        /* ================= MAIN ================= */

        .container {
            width: 100%;
            max-width: 1100px;

            margin: 0 auto;

            padding: 40px 20px;
        }

        .page {
            display: none;
        }

        .page.active {
            display: block;
            animation: showPage 0.25s ease;
        }

        @keyframes showPage {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ================= HOME ================= */

        .home {
            min-height: 70vh;

            display: flex;
            flex-direction: column;

            align-items: center;
            justify-content: center;

            text-align: center;
        }

        .home h1 {
            color: #2563eb;

            font-size: clamp(55px, 10vw, 100px);

            margin-bottom: 20px;
        }

        .home-text {
            max-width: 650px;

            color: #64748b;

            font-size: 20px;

            line-height: 1.6;
        }

        .welcome {
            margin-top: 35px;

            background: white;

            padding: 30px 40px;

            border-radius: 20px;

            box-shadow:
                0 10px 30px rgba(0, 0, 0, 0.07);
        }

        .welcome h2 {
            margin-bottom: 10px;
        }

        .welcome p {
            color: #64748b;
        }

        /* ================= TITLE ================= */

        .page-title {
            margin-bottom: 30px;
        }

        .page-title h2 {
            font-size: 36px;
            margin-bottom: 8px;
        }

        .page-title p {
            color: #64748b;
        }

        /* ================= CARDS ================= */

        .cards {
            display: grid;

            grid-template-columns:
                repeat(3, 1fr);

            gap: 20px;
        }

        .card {
            background: white;

            padding: 30px;

            border-radius: 18px;

            border: 1px solid #e5e7eb;

            box-shadow:
                0 8px 25px rgba(0, 0, 0, 0.05);
        }

        .card-icon {
            font-size: 40px;

            margin-bottom: 15px;
        }

        .card h3 {
            margin-bottom: 10px;
        }

        .card p {
            color: #64748b;
            line-height: 1.5;
        }

        /* ================= IMAGES ================= */

        .empty-gallery {
            background: white;

            border: 2px dashed #d1d5db;

            border-radius: 20px;

            padding: 80px 20px;

            text-align: center;
        }

        .empty-gallery-icon {
            font-size: 60px;

            margin-bottom: 15px;
        }

        .empty-gallery h3 {
            font-size: 24px;

            margin-bottom: 10px;
        }

        .empty-gallery p {
            color: #64748b;
        }

        /* ================= PROFILE ================= */

        .profile-box {
            max-width: 650px;

            background: white;

            padding: 35px;

            border-radius: 20px;

            box-shadow:
                0 10px 30px rgba(0, 0, 0, 0.06);
        }

        .avatar {
            width: 90px;
            height: 90px;

            border-radius: 50%;

            background: #eff6ff;

            display: flex;
            align-items: center;
            justify-content: center;

            font-size: 40px;

            color: #2563eb;

            margin-bottom: 20px;
        }

        .profile-box h2 {
            margin-bottom: 10px;
        }

        .profile-box p {
            color: #64748b;

            line-height: 1.6;
        }

        /* ================= LINKS ================= */

        .links {
            display: flex;
            flex-direction: column;

            gap: 15px;
        }

        .link-card {
            background: white;

            padding: 20px;

            border-radius: 15px;

            display: flex;

            align-items: center;
            justify-content: space-between;

            gap: 20px;

            box-shadow:
                0 8px 25px rgba(0, 0, 0, 0.05);
        }

        .link-info h3 {
            margin-bottom: 6px;
        }

        .link-info p {
            color: #64748b;

            font-size: 14px;
        }

        .open-link {
            background: #2563eb;

            color: white;

            text-decoration: none;

            padding: 11px 18px;

            border-radius: 9px;

            white-space: nowrap;
        }

        /* ================= SETTINGS ================= */

        .settings {
            max-width: 650px;

            background: white;

            padding: 30px;

            border-radius: 20px;

            box-shadow:
                0 10px 30px rgba(0, 0, 0, 0.06);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;

            margin-bottom: 8px;

            font-weight: 700;
        }

        .form-group input {
            width: 100%;

            padding: 13px 15px;

            border: 1px solid #d1d5db;

            border-radius: 10px;

            font-size: 16px;

            outline: none;
        }

        .form-group input:focus {
            border-color: #2563eb;

            box-shadow:
                0 0 0 3px rgba(37, 99, 235, 0.1);
        }

        .save-button {
            border: none;

            background: #2563eb;

            color: white;

            padding: 13px 24px;

            border-radius: 10px;

            font-size: 15px;

            font-weight: 700;

            cursor: pointer;
        }

        .save-button:hover {
            background: #1d4ed8;
        }

        /* ================= FOOTER ================= */

        footer {
            margin-top: 40px;

            background: #111827;

            color: #9ca3af;

            text-align: center;

            padding: 25px;
        }

        /* ================= MOBILE ================= */

        @media (max-width: 850px) {

            .header-inner {
                flex-direction: column;

                align-items: stretch;

                padding: 15px;
            }

            .logo {
                text-align: center;
            }

            .nav {
                width: 100%;

                overflow-x: auto;

                padding-bottom: 4px;

                justify-content: flex-start;
            }

            .nav-button {
                flex-shrink: 0;
            }

            .cards {
                grid-template-columns: 1fr;
            }

            .link-card {
                flex-direction: column;

                align-items: flex-start;
            }
        }

        @media (max-width: 500px) {

            .container {
                padding: 25px 15px;
            }

            .page-title h2 {
                font-size: 30px;
            }

            .welcome {
                padding: 25px 20px;
            }

            .settings {
                padding: 20px;
            }
        }
    </style>
</head>


<body>


<!-- ================= HEADER ================= -->

<header class="header">

    <div class="header-inner">

        <div
            class="logo"
            onclick="openPage('home')"
        >
            EasyFlow
        </div>


        <nav class="nav">

            <button
                class="nav-button active"
                data-page="home"
                onclick="openPage('home')"
            >
                🏠 Главная
            </button>


            <button
                class="nav-button"
                data-page="images"
                onclick="openPage('images')"
            >
                🖼️ Картинки
            </button>


            <button
                class="nav-button"
                data-page="profile"
                onclick="openPage('profile')"
            >
                👤 Профиль
            </button>


            <button
                class="nav-button"
                data-page="links"
                onclick="openPage('links')"
            >
                🔗 Мои ссылки
            </button>


            <button
                class="nav-button"
                data-page="settings"
                onclick="openPage('settings')"
            >
                ⚙️ Настройки
            </button>

        </nav>

    </div>

</header>


<!-- ================= CONTENT ================= -->

<main class="container">


    <!-- ===== HOME ===== -->

    <section
        id="home"
        class="page active"
    >

        <div class="home">

            <h1>
                EasyFlow
            </h1>

            <p class="home-text">
                Твоя собственная платформа.
                Создавай свой контент,
                добавляй ссылки и настраивай
                свой профиль.
            </p>


            <div class="welcome">

                <h2>
                    Добро пожаловать! 👋
                </h2>

                <p>
                    EasyFlow только начинается.
                </p>

            </div>

        </div>

    </section>


    <!-- ===== IMAGES ===== -->

    <section
        id="images"
        class="page"
    >

        <div class="page-title">

            <h2>
                🖼️ Картинки
            </h2>

            <p>
                Здесь будут находиться твои изображения.
            </p>

        </div>


        <div class="empty-gallery">

            <div class="empty-gallery-icon">
                🖼️
            </div>

            <h3>
                Пока нет картинок
            </h3>

            <p>
                Позже здесь можно будет добавлять свои изображения.
            </p>

        </div>

    </section>


    <!-- ===== PROFILE ===== -->

    <section
        id="profile"
        class="page"
    >

        <div class="page-title">

            <h2>
                👤 Профиль
            </h2>

            <p>
                Твоя персональная страница.
            </p>

        </div>


        <div class="profile-box">

            <div class="avatar">
                👤
            </div>


            <h2 id="profileName">
                Мой профиль
            </h2>


            <p id="profileDescription">
                Здесь будет информация о пользователе.
            </p>

        </div>

    </section>


    <!-- ===== LINKS ===== -->

    <section
        id="links"
        class="page"
    >

        <div class="page-title">

            <h2>
                🔗 Мои ссылки
            </h2>

            <p>
                Здесь можно размещать свои ссылки.
            </p>

        </div>


        <div class="links">


            <div class="link-card">

                <div class="link-info">

                    <h3>
                        Моя ссылка
                    </h3>

                    <p>
                        Добавь сюда свою ссылку.
                    </p>

                </div>


                <a
                    href="#"
                    class="open-link"
                >
                    Открыть
                </a>

            </div>


        </div>

    </section>


    <!-- ===== SETTINGS ===== -->

    <section
        id="settings"
        class="page"
    >

        <div class="page-title">

            <h2>
                ⚙️ Настройки
            </h2>

            <p>
                Измени информацию своего профиля.
            </p>

        </div>


        <div class="settings">


            <div class="form-group">

                <label for="name">
                    Имя
                </label>

                <input
                    id="name"
                    type="text"
                    placeholder="Например: Alex"
                >

            </div>


            <div class="form-group">

                <label for="description">
                    Описание
                </label>

                <input
                    id="description"
                    type="text"
                    placeholder="Расскажи о себе"
                >

            </div>


            <button
                class="save-button"
                onclick="saveProfile()"
            >
                Сохранить
            </button>

        </div>

    </section>


</main>


<!-- ================= FOOTER ================= -->

<footer>

    © 2026 EasyFlow

</footer>


<script>

    /* ==========================================
       ПЕРЕКЛЮЧЕНИЕ РАЗДЕЛОВ
    ========================================== */

    function openPage(pageName) {

        /* Скрываем все страницы */

        const pages =
            document.querySelectorAll(".page");

        pages.forEach(function(page) {

            page.classList.remove("active");

        });


        /* Показываем выбранную */

        const selectedPage =
            document.getElementById(pageName);

        if (selectedPage) {

            selectedPage.classList.add("active");

        }


        /* Меняем активную кнопку */

        const buttons =
            document.querySelectorAll(".nav-button");

        buttons.forEach(function(button) {

            button.classList.remove("active");

            if (
                button.getAttribute("data-page")
                === pageName
            ) {

                button.classList.add("active");

            }

        });


        /* Прокручиваем вверх */

        window.scrollTo({
            top: 0,
            behavior: "smooth"
        });

    }


    /* ==========================================
       СОХРАНЕНИЕ ПРОФИЛЯ
    ========================================== */

    function saveProfile() {

        const name =
            document.getElementById("name").value.trim();


        const description =
            document
                .getElementById("description")
                .value
                .trim();


        if (name) {

            localStorage.setItem(
                "easyflow_name",
                name
            );

            document
                .getElementById("profileName")
                .textContent = name;

        }


        if (description) {

            localStorage.setItem(
                "easyflow_description",
                description
            );

            document
                .getElementById("profileDescription")
                .textContent = description;

        }


        alert("Профиль сохранён!");

    }


    /* ==========================================
       ЗАГРУЗКА ПРОФИЛЯ
    ========================================== */

    window.addEventListener(
        "DOMContentLoaded",
        function() {

            const savedName =
                localStorage.getItem(
                    "easyflow_name"
                );


            const savedDescription =
                localStorage.getItem(
                    "easyflow_description"
                );


            if (savedName) {

                document
                    .getElementById("name")
                    .value = savedName;

                document
                    .getElementById("profileName")
                    .textContent = savedName;

            }


            if (savedDescription) {

                document
                    .getElementById("description")
                    .value = savedDescription;

                document
                    .getElementById("profileDescription")
                    .textContent =
                    savedDescription;

            }

        }
    );

</script>


</body>
</html>
